using UnityEngine;

public class ArmCameraView : CameraMode
{
    public override string GetName()
    {
        return "Arm Camera";
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
